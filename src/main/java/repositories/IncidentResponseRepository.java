package repositories;

import entity.IncidentResponse;
import org.springframework.data.jpa.repository.JpaRepository;

public interface IncidentResponseRepository extends JpaRepository<IncidentResponse, Long> {
}
